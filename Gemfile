source 'https://rubygems.org'

gem 'rake'
gem 'rspec'
gem 'require_all'
gem 'byebug'

# You'll need these installed for scripts/
# But it causes problems on installation so
# we don't need it for everyone
# gem 'mechanize'
